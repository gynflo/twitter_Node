div(class="search-menu d-flex flex-column")
    if users && users.length 
        each user in users 
            a(class="d-flex flex-row align-items-center search-menu-item pointer-cursor" href="/users/" + user.username)
                img(src=user.avatar width="40" height="40" class="p-1")
                span(class="pl-3")=user.username
    else 
        span(class="p-3") Aucun Utilisateur